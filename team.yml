# Allowlist branch of the two-branch front funnel (Option A).
#
# An item counts as front when EITHER:
#   1. Microsoft.VSTS.Common.Activity == 'Front End (BUG)'  (board field,
#      UI label "Tipo de Servico"), OR
#   2. System.AssignedTo displayName is listed in `frontDevs` below.
#
# Branch 1 is the correct board convention and covers the whole front repo
# (patients and medicos, one codebase). Branch 2 is a rescue: the patients
# squad fills the Activity field unreliably (~1 in 3 items), so their front is
# recovered by developer name. The medicos squad fills the field correctly and
# needs no allowlist. A dev joining or leaving the patients squad edits this
# file, never code. Names must match the Azure DevOps display name exactly
# (org hiae).
#
# Referenced by .claude/skills/extract-board-metrics/board-model.md.

frontDevs:
  - Lucas Gabriel da Silva
  - Samuel Soares da Rocha
  - Joao Carlos Rodrigues Dias
  - Edilson Aparecido Rodrigues

# No medicos allowlist: medicos front is identified entirely by branch 1
# (the Activity field). If the medicos squad ever regresses on filling the
# field, add their display names here as a rescue, mirroring the patients row.
